// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { RequestError } from "./RequestError";
import type { RequestId } from "./RequestId";
import type { ResponseBody } from "./ResponseBody";

/**
 * Messages sent from the server to a client.
 */
export type ServerMessage = { "type": "welcome", 
/**
 * The server's version.
 */
server_version: string, 
/**
 * The server's [`PROTOCOL_VERSION`](crate::PROTOCOL_VERSION). A client that speaks
 * another version cannot use this server.
 */
protocol_version: number, 
/**
 * The generator version the server runs, which new universes are created with.
 */
generator_version: number, } | { "type": "pong", 
/**
 * The ping's nonce.
 */
nonce: number, } | { "type": "error", 
/**
 * What was wrong, for the client's log.
 */
message: string, } | { "type": "response", 
/**
 * The request's ID.
 */
id: RequestId, 
/**
 * The answer, whose `kind` is the request's.
 */
body: ResponseBody, } | { "type": "request_error", 
/**
 * The request's ID.
 */
id: RequestId, 
/**
 * Why it failed.
 */
error: RequestError, };
