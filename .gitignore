# Rust
target/

# Fetched datasets of the fitting crate (plan 15, Design note 12): only their PROVENANCE.toml is
# committed, under crates/hyperion-fit/data/<dataset>/.
crates/hyperion-fit/data/cache/

# Stray ts-rs output: a `cargo test` that misses `.cargo/config.toml`'s TS_RS_EXPORT_DIR writes the
# bindings here instead of packages/protocol/src/generated.
/bindings/

# Server data (saved universes) from `just server`
hyperion-data/

# Node
node_modules/
*.tsbuildinfo

# Python (skill scripts under .claude/skills/)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Build output
out/
dist/
coverage/

# Local environment
.env
.env.*.local
*.log
.DS_Store
