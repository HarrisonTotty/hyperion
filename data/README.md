# HYPERION Data Directory

The following directory contains the default game data used in HYPERION. This directory system allows one to easily extend or otherwise replace all of the weapons, ship classes, factions, etc!

## `modules.yaml`

This file defines the general properties of each different kind of ship module that may be added to a ship during the build process.